
export function useFlatListStyle(logic: any) {
  return {
    container: {
      // flex: 1 ensures the list expands to fill the available vertical space in its parent container.
      flex: 1,
    }
  };
}
