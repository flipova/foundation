# Default registry: npm (public)
registry=https://registry.npmjs.org/

# GitHub Packages auth for @flipova scope (optional: used when installing from ghcr)
# Uncomment and set token to install from GitHub Packages:
# @flipova:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
